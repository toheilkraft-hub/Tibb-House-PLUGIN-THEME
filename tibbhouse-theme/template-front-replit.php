<?php
/**
 * Template Name: Homepage (Replit Design)
 *
 * Page template used by the auto-created "TIBB FRONT PAGE REPLIT" page
 * (see inc/homepage-install.php). Renders the exact same homepage layout
 * as front-page.php so the site owner can assign this Page as the static
 * homepage from Settings → Reading and immediately see the full design,
 * with all content (Treatments, Conditions, Practitioners, etc.) pulled
 * live from the Tibb House Core plugin.
 *
 * @package Tibbhouse
 */

get_header();
tibbhouse_render_homepage();
get_footer();
